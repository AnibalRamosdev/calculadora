name: calculadora_figma
description: "Proyecto de calculadora con diseño Figma"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '^3.5.0' # <--- Esto soluciona el error que te apareció

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  math_expressions: ^2.6.0 # <--- Para que funcione la lógica
  image_picker: ^1.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true